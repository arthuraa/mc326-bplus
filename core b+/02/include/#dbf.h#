#ifndef __DBF_H
#define __DBF_H

#include <stdio.h>

size_t fwritep(const void*, size_t, long, int, FILE*);
size_t freadp(void*, size_t, long, int, FILE*);
size_t fwrited(const void*, size_t, long, int, FILE*);
size_t freadd(void*, size_t*, long, int, FILE*);

#endif

